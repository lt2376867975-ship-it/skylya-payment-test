# 2026-08-27 GitHub 付费邀请码源码发布

## 时间与目标

- 本地时间：2026-08-27 18:45:51 +08:00。
- 工作包基线：2026-08-27 Skylya V6 当前工作包；本机绝对路径不写入公开仓库。
- 目标仓库：`lt2376867975-ship-it/skylya-payment-test`，公开仓库。
- 目标：把 2026-08-27 已上线的付费一次性邀请码原型整理为安全、可审阅的功能增量源码快照并发布到 GitHub。

## 发布范围

- GitHub `main` 首个源码提交信息：`feat: publish paid invite payment prototype`；提交哈希以完成公开仓库安全收口后的远端记录为准。
- 采用功能增量快照，不上传完整 Skylya 工作包；仓库需要叠加到匹配的 Skylya V6 基线，不能单独视为完整产品源码。
- 54 个业务与交接文件逐一从当前工作包复制，覆盖：
  - 官网 `PaidInvitePurchase`、`AppView` 和相关样式。
  - 用户端登录/注册页、微信手机号验证页和 `AuthApi` 邀请码参数链路。
  - Auth/User/Gateway、公共错误码与内部服务白名单相关源码。
  - 邀请码签发、短信提交、注册校验与原子核销的聚焦测试。
  - 两份迁移 SQL、两份 schema 快照、Compose 环境变量透传和原正式发布 change-log。
- 另含仓库级 `README.md` 与 `.gitignore`，明确这是模拟付款原型、真实支付仍未接入。

## 安全与排除项

- 仓库公开，因此没有上传 `.env`、阿里云密钥、GitHub Token、数据库连接串、服务器备份、数据库导出、运行日志、发布归档、JAR、APK、签名文件、`node_modules`、`dist` 或 `target`。
- 提交前扫描未命中 GitHub/OpenAI/阿里云凭据前缀、私钥头、内网 URL 或禁止扩展名。
- 代码和测试中保留的手机号格式值是固定模拟样例；未包含生产验收手机号。
- `application.yml` 与 `docker-compose.yml` 中相关敏感配置均保持环境变量绑定，没有写入运行值。

## 验证

- 54 个工作包源文件与上传快照逐文件 SHA-256 对比：`IDENTITY_MISMATCHES=0`。
- GitHub 首个提交已成功推送到 `origin/main`。
- 空仓库首次提交会把所有快照文件视为新增，`git diff --cached --check` 报告的行尾空格来自原工作包既有文件；为保持发布源码字节一致，本轮没有顺手格式化这些业务文件。
- 功能测试、构建和生产验证沿用 `project-docs/change-logs/2026-08-27-付费一次性邀请码原型.md` 的正式发布证据，本轮 GitHub 发布没有重新修改业务代码、服务器或数据库。

## 未操作

- 未向原用户端 Gitee 仓库推送，未改变其 `origin/master`。
- 未重新部署、重启服务或修改 MySQL、MongoDB、Redis、Qdrant、uploads/OSS。
- 未接真实支付渠道；仓库中的“我已付款”仍是受配置开关保护的模拟入口。
